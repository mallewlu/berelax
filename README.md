# Be Relax — Plexa

Social wellness platform for Gen Z

## Setup

```bash
npm install
```

Copy `.env.example` to `.env` and fill in your Supabase credentials:

```bash
cp .env.example .env
```

Then run the development server:

```bash
npm run dev
```

## Deploy

Connected to Netlify — auto-deploys on push to main
